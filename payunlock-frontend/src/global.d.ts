// No need for ethereum window property anymore as we're using Hedera
// If needed, we can add Hedera-specific global types here in the future

// This is a placeholder for any future Hedera-specific global types
interface Window {
  // Removed ethereum property as we're now using Hedera
}
